import { StyleSheet } from "react-native";

export const GlobalStye = StyleSheet.create({
  container: {
    display: "flex",
    flex: 1,
  },
  wrapper: {
    display: "flex",
    flex: 1,
    paddingHorizontal: 12,
  },
});
